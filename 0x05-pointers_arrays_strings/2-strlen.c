#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: value of the parameter
 * Return: length
 */
int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
